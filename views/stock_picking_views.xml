<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        Extensión propia de Torre de Control.

        Este archivo NO crea botones nuevos de Packing List / Worksheet.
        Los botones originales ya existen en stock_lot_packing_import.

        Aquí solo agregamos:
        - Campo técnico tc_is_physical_reception
        - Smart button de tránsito
        - Pedidos consolidados

        Y después reactivamos un parche de visibilidad sobre los botones originales.
    -->
    <record id="view_picking_form_transit_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.transit.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="priority">40</field>
        <field name="arch" type="xml">

            <field name="partner_id" position="after">
                <field name="tc_is_physical_reception" invisible="1"/>
            </field>

            <xpath expr="//header" position="inside">
                <field name="tc_reception_voyage_id" invisible="1"/>
                <!-- Recepción equivocada (validada+devuelta o cancelada):
                     libera el embarque para reasignar las placas del PL a
                     una recepción NUEVA. Vive aquí (y no en
                     stock_lot_packing_import) porque este módulo es el dueño
                     de supplier_shipment_id. -->
                <field name="supplier_shipment_id" invisible="1"/>
                <button name="action_som_release_shipment_link"
                        string="Reasignar PL a nueva recepción"
                        type="object"
                        icon="fa-refresh"
                        confirm="Esta recepción se desvinculará del embarque del portal y las placas del PL se reasignarán a una recepción NUEVA. ¿Continuar?"
                        invisible="state not in ('done', 'cancel') or not supplier_shipment_id"
                        groups="stock.group_stock_user"/>

                <button name="action_tc_close_demand"
                        type="object"
                        string="Cerrar demanda"
                        class="btn-warning"
                        groups="stock.group_stock_user"
                        invisible="not tc_reception_voyage_id or state in ('done', 'cancel')"
                        confirm="CERRAR LA DEMANDA del embarque: el material que aún no llega quedará CANCELADO y el embarque se dará por terminado. Esta es la ÚNICA forma de matar el pendiente — ¿confirmas?"/>
                <button name="action_print_reception_labels"
                        string="7. Imprimir Etiquetas"
                        type="object"
                        class="btn-secondary"
                        invisible="not tc_is_physical_reception"/>
            </xpath>

            <!-- Barra de estatus del flujo 0-7: espejo de la botonera
                 numerada, entre el header y la hoja. También en las
                 recepciones físicas de Torre de Control (tipo interno). -->
            <xpath expr="//sheet" position="before">
                <div invisible="picking_type_code != 'incoming' and not tc_is_physical_reception">
                    <field name="worksheet_printed" invisible="1"/>
                    <field name="tc_label_print_count" invisible="1"/>
                    <widget name="reception_flow_bar"/>
                </div>
            </xpath>

            <div name="button_box" position="inside">
                <button name="action_view_transit_voyage"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-ship"
                        invisible="transit_count == 0">
                    <field name="transit_count"
                           widget="statinfo"
                           string="En Tránsito"/>
                </button>
            </div>

            <xpath expr="//field[@name='origin']" position="after">
                <field name="transit_sale_order_ids"
                       widget="many2many_tags"
                       invisible="picking_type_code != 'incoming'"
                       string="Pedidos (Consolidado)"/>
            </xpath>

        </field>
    </record>


    <!--
        Parche sobre los botones ORIGINALES del módulo principal:
        stock_lot_packing_import.view_picking_form_inherit_packing_import

        No se crea ningún botón nuevo.
        Solo se ajusta la visibilidad de los botones existentes para que también
        aparezcan en recepciones físicas internas de Torre de Control.

        Importante Odoo 19:
        - No usar @string como selector XPath.
        - No usar atributos invisible multilínea.
    -->
    <record id="view_picking_form_transit_patch_packing_import_buttons" model="ir.ui.view">
        <field name="name">stock.picking.form.transit.patch.packing.import.buttons</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock_lot_packing_import.view_picking_form_inherit_packing_import"/>
        <field name="priority">99</field>
        <field name="active" eval="True"/>
        <field name="arch" type="xml">

            <!-- Campo técnico disponible dentro de la rama heredada del módulo principal. -->
            <xpath expr="//field[@name='supplier_access_ids']" position="after">
                <field name="tc_is_physical_reception" invisible="1"/>
            </xpath>

            <!-- Abrir PL: primer botón action_open_packing_list_spreadsheet del módulo principal. -->
            <xpath expr="(//header/button[@name='action_open_packing_list_spreadsheet'])[1]" position="attributes">
                <attribute name="invisible">state in ('done', 'cancel', 'draft') or packing_list_imported or worksheet_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>

            <!-- Procesar PL: primer botón action_import_packing_list del módulo principal. -->
            <xpath expr="(//header/button[@name='action_import_packing_list'])[1]" position="attributes">
                <attribute name="invisible">state in ('done', 'cancel', 'draft') or packing_list_imported or not spreadsheet_id or worksheet_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>

            <!-- Corregir PL: segundo botón action_open_packing_list_spreadsheet del módulo principal. -->
            <xpath expr="(//header/button[@name='action_open_packing_list_spreadsheet'])[2]" position="attributes">
                <attribute name="invisible">state in ('done', 'cancel', 'draft') or not packing_list_imported or worksheet_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>

            <!-- Reprocesar PL: segundo botón action_import_packing_list del módulo principal. -->
            <xpath expr="(//header/button[@name='action_import_packing_list'])[2]" position="attributes">
                <attribute name="invisible">state in ('done', 'cancel', 'draft') or not packing_list_imported or worksheet_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>


            <!-- Imprimir Worksheet original. El Worksheet SIGUE imprimible
                 después de validar la recepción (estado done): es el
                 documento de trabajo del almacén y se reimprime a demanda. -->
            <xpath expr="//header/button[@name='action_print_worksheet_pdf']" position="attributes">
                <attribute name="invisible">state in ('cancel', 'draft') or not packing_list_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>

            <!-- Abrir Worksheet original. -->
            <xpath expr="//header/button[@name='action_open_worksheet_spreadsheet']" position="attributes">
                <attribute name="invisible">state in ('done', 'cancel', 'draft') or not packing_list_imported or worksheet_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>

            <!-- Procesar Worksheet original. -->
            <xpath expr="//header/button[@name='action_import_worksheet']" position="attributes">
                <attribute name="invisible">state in ('done', 'cancel', 'draft') or not packing_list_imported or not ws_spreadsheet_id or worksheet_imported or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>

            <!-- 0. Comenzar y 6. Validar también en recepciones físicas
                 internas de Torre de Control (los nativos sin número se
                 ocultan ahí igual que en incoming).
                 Orden en el arch combinado: action_assign [1]=nativo,
                 [2]=0. Comenzar; button_validate [1]=6. Validar (insertado
                 tras action_assign), [2]=nativo oe_highlight, y el nativo
                 o_btn_validate se distingue por hasclass. -->
            <xpath expr="(//header/button[@name='action_assign'])[1]" position="attributes">
                <attribute name="invisible">not show_check_availability or picking_type_code == 'incoming' or tc_is_physical_reception</attribute>
            </xpath>
            <xpath expr="(//header/button[@name='action_assign'])[2]" position="attributes">
                <attribute name="invisible">not show_check_availability or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>
            <xpath expr="(//header/button[@name='button_validate'])[1]" position="attributes">
                <attribute name="invisible">state in ('draft', 'done', 'cancel') or (picking_type_code != 'incoming' and not tc_is_physical_reception)</attribute>
            </xpath>
            <xpath expr="(//header/button[@name='button_validate'])[2]" position="attributes">
                <attribute name="invisible">state in ('draft', 'confirmed', 'done', 'cancel') or picking_type_code == 'incoming' or tc_is_physical_reception</attribute>
            </xpath>
            <xpath expr="//header/button[@name='button_validate'][hasclass('o_btn_validate')]" position="attributes">
                <attribute name="invisible">state in ('waiting', 'assigned', 'done', 'cancel') or picking_type_code == 'incoming' or tc_is_physical_reception</attribute>
            </xpath>

        </field>
    </record>

    <!-- Regenerar PL físico: en el MENÚ DE ACCIONES (engrane) del formulario,
         no como botón de header. Reconstruye la plantilla con las Ref.
         Internas VIGENTES del viaje (imprescindible tras un renumerado de
         lotes; la plantilla vieja cruza el pairing). El método valida que
         sea recepción física abierta. -->
    <record id="action_regenerate_physical_pl_spreadsheet" model="ir.actions.server">
        <field name="name">Regenerar PL físico</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_type">action</field>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action = records.action_regenerate_physical_pl_spreadsheet()</field>
    </record>

    <!-- Renumerar lotes por producto (engrane): bloques corridos 01..N por
         producto dentro de cada contenedor. Repara embarques que quedaron
         con series intercaladas (pares/nones) por el renumerado previo al
         fix del orden. -->
    <record id="action_tc_renumber_lots_by_product" model="ir.actions.server">
        <field name="name">Renumerar lotes por producto</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_type">action</field>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action = records.action_tc_renumber_lots_by_product()</field>
    </record>

</odoo>