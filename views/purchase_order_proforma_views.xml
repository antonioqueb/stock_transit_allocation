<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_order_form_inherit_proforma" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.proforma</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_proforma_header"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-file-text"
                        invisible="state not in ('purchase', 'done')">
                    <div class="o_stat_info">
                        <field name="proforma_shipment_count" class="o_stat_value"/>
                        <span class="o_stat_text">Embarques</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>