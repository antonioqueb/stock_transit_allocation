<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- MANTENER LIST VIEW Y KANBAN IGUAL, SOLO EDITAMOS FORM VIEW -->

    <!-- LIST VIEW (Igual que antes) -->
    <record id="view_stock_transit_voyage_list" model="ir.ui.view">
        <field name="name">stock.transit.voyage.list</field>
        <field name="model">stock.transit.voyage</field>
        <field name="arch" type="xml">
            <list decoration-info="state == 'in_transit'" decoration-success="state == 'arrived'">
                <field name="name"/>
                <field name="container_number" string="Contenedor(es)"/>
                <field name="vessel_name"/>
                <field name="eta" widget="remaining_days"/>
                <field name="total_m2" sum="Total"/>
                <field name="allocation_percent" widget="progressbar"/>
                <field name="state" widget="badge" decoration-info="state == 'in_transit'" decoration-warning="state == 'at_port'"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="view_stock_transit_voyage_form" model="ir.ui.view">
        <field name="name">stock.transit.voyage.form</field>
        <field name="model">stock.transit.voyage</field>
        <field name="arch" type="xml">
            <form string="Gestión de Tránsito">
                <header>
                    <button name="action_load_from_picking" string="🔄 Recargar Datos" type="object" class="btn-secondary" invisible="state == 'arrived'"/>
                    <button name="action_confirm_transit" string="🚢 Confirmar Zarpe" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_arrive" string="🏁 Registrar Llegada" type="object" class="btn-success" invisible="state != 'in_transit'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_transit,at_port,arrived"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                        <h3>
                            <!-- Campo informativo que agrupa los contenedores encontrados -->
                            <field name="container_number" placeholder="Contenedor(es)"/>
                        </h3>
                    </div>
                    <group>
                        <group string="Detalles de Logística">
                            <field name="vessel_name"/>
                            <field name="voyage_number"/>
                            <field name="bl_number"/>
                            <field name="picking_id" readonly="1"/>
                        </group>
                        <group string="Itinerario">
                            <field name="etd"/>
                            <field name="eta"/>
                            <field name="arrival_date" readonly="1"/>
                            <field name="transit_progress" widget="transit_ship_progress"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Contenido y Asignación" name="lots">
                            <field name="line_ids">
                                <list create="0" delete="0" decoration-success="allocation_status == 'reserved'">
                                    <!-- AQUI AGREGAMOS EL CONTENEDOR POR LINEA -->
                                    <field name="container_number" optional="show"/> 
                                    <field name="product_id"/>
                                    <field name="lot_id"/>
                                    <field name="x_grosor" optional="hide"/>
                                    <field name="x_alto" optional="hide"/>
                                    <field name="x_ancho" optional="hide"/>
                                    <field name="product_uom_qty" string="m²"/>
                                    <field name="partner_id" widget="many2one_avatar"/>
                                    <field name="allocation_status" widget="badge" decoration-success="allocation_status == 'reserved'" decoration-info="allocation_status == 'available'"/>
                                    <button name="action_reassign_wizard" string="🔄 Reasignar" type="object" class="btn-link"/>
                                </list>
                            </field>
                        </page>
                        <page string="Estadísticas">
                            <group>
                                <field name="total_m2"/>
                                <field name="allocated_m2"/>
                                <field name="allocation_percent" widget="progressbar"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    
    <!-- KANBAN y ACTION se mantienen igual que en tu código anterior -->
    <!-- ... -->
</odoo>