<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================== -->
    <!-- VISTAS DEL MODELO STOCK.TRANSIT.VOYAGE (VIAJES)           -->
    <!-- ========================================================== -->
    
    <!-- 1. VISTA DE LISTA (VIAJES) -->
    <record id="view_stock_transit_voyage_list" model="ir.ui.view">
        <field name="name">stock.transit.voyage.list</field>
        <field name="model">stock.transit.voyage</field>
        <field name="arch" type="xml">
            <list decoration-info="state == 'in_transit'" decoration-success="state == 'arrived'">
                <field name="name"/>
                <field name="custom_status" widget="badge" decoration-warning="custom_status=='production'" decoration-info="custom_status=='on_sea'"/>
                <field name="container_number" string="Contenedores"/>
                <field name="bl_number"/>
                <field name="shipping_line"/>
                <field name="eta" widget="remaining_days"/>
                <field name="allocation_percent" widget="progressbar" string="% Asig."/>
                <field name="state" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- 2. VISTA DE FORMULARIO (VIAJES) -->
    <record id="view_stock_transit_voyage_form" model="ir.ui.view">
        <field name="name">stock.transit.voyage.form</field>
        <field name="model">stock.transit.voyage</field>
        <field name="arch" type="xml">
            <form string="Gestión de Tránsito">
                <header>
                    <button name="action_load_from_picking" string="🔄 Recargar Datos" type="object" class="btn-secondary" invisible="state == 'arrived'"/>
                    <button name="action_confirm_transit" string="🚢 Confirmar Zarpe" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_arrive" string="🏁 Registrar Llegada" type="object" class="btn-success" invisible="state != 'in_transit'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_transit,at_port,arrived"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                        <h3><field name="container_number" placeholder="Contenedor(es)"/></h3>
                    </div>
                    <group>
                        <group string="Logística y Estatus">
                            <field name="custom_status"/>
                            <field name="shipping_line" placeholder="Ej. MSC"/>
                            <field name="vessel_name"/>
                            <field name="bl_number"/>
                            <field name="picking_id" readonly="1"/>
                        </group>
                        <group string="Itinerario">
                            <field name="transit_days_expected"/>
                            <field name="etd"/>
                            <field name="eta"/>
                            <field name="arrival_date" readonly="1"/>
                            <field name="transit_progress" widget="transit_ship_progress"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Contenido y Asignación" name="lots">
                            <field name="line_ids">
                                <list create="0" delete="0" decoration-success="allocation_status == 'reserved'" editable="bottom">
                                    <field name="container_number" optional="show"/> 
                                    <field name="product_id"/>
                                    <field name="lot_id"/>
                                    <field name="product_uom_qty" string="m²"/>
                                    <field name="partner_id" widget="many2one_avatar"/>
                                    <field name="order_id" optional="show" widget="badge"/>
                                    <field name="notes" optional="hide"/>
                                    <field name="allocation_status" widget="badge"/>
                                    <button name="action_reassign_wizard" string="🔄" type="object" class="btn-link"/>
                                </list>
                            </field>
                        </page>
                        <page string="Estadísticas">
                            <group>
                                <field name="total_m2"/>
                                <field name="allocated_m2"/>
                                <field name="allocation_percent" widget="progressbar"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- 3. VISTA KANBAN (VIAJES) -->
    <record id="view_stock_transit_voyage_kanban" model="ir.ui.view">
        <field name="name">stock.transit.voyage.kanban</field>
        <field name="model">stock.transit.voyage</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column" records_draggable="0" sample="1">
                <field name="state"/>
                <field name="name"/>
                <field name="vessel_name"/>
                <field name="shipping_line"/>
                <field name="custom_status"/>
                <field name="container_number"/>
                <field name="eta"/>
                <field name="allocation_percent"/>
                <field name="transit_progress"/> 
                <templates>
                    <t t-name="card">
                        <div class="d-flex flex-column">
                            <div class="d-flex justify-content-between mb-2">
                                <strong class="o_kanban_record_title text-primary">
                                    <field name="name"/>
                                </strong>
                                <field name="custom_status" widget="label_selection" options="{'classes': {'production': 'warning', 'on_sea': 'info', 'delivered': 'success'}}"/>
                            </div>
                            <div class="text-muted mb-1" invisible="not shipping_line">
                                <small><field name="shipping_line"/></small>
                            </div>
                            <div class="text-muted mb-1" invisible="not vessel_name">
                                <i class="fa fa-ship me-1"/> <field name="vessel_name"/>
                            </div>
                            <div class="d-flex justify-content-between align-items-center mt-auto">
                                <span class="text-muted small">
                                    ETA: <field name="eta" widget="remaining_days"/>
                                </span>
                            </div>
                            <div class="mt-2" title="Porcentaje Asignado">
                                <small>Asignado:</small>
                                <field name="allocation_percent" widget="progressbar"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ========================================================== -->
    <!-- NUEVA VISTA: SÁBANA DE SEGUIMIENTO (EXCEL REPLICATION)    -->
    <!-- Apunta a stock.transit.line para desglose detallado       -->
    <!-- ========================================================== -->

    <record id="view_transit_line_tracking_list" model="ir.ui.view">
        <field name="name">stock.transit.line.tracking.list</field>
        <field name="model">stock.transit.line</field>
        <field name="arch" type="xml">
            <list string="Sábana de Seguimiento" create="0" delete="0" edit="1" editable="bottom"
                  decoration-success="voyage_status == 'delivered'"
                  decoration-info="voyage_status == 'on_sea'"
                  decoration-warning="voyage_status == 'production'">
                
                <!-- 1. OC SISTEMA / FECHA -->
                <field name="purchase_id" string="OC SISTEMA" optional="show"/>
                <field name="date_order" string="FECHA OC" widget="date" optional="show"/>
                
                <!-- 2. STATUS / VENDEDOR -->
                <field name="voyage_status" string="Status"/>
                <field name="salesperson_id" string="Vendedor" widget="many2one_avatar_user"/>
                
                <!-- 3. SALES ORDER / CLIENTE -->
                <field name="order_id" string="SALES ORDER"/>
                <field name="partner_id" string="Cliente / Proyecto"/>
                
                <!-- 4. INFO PROVEEDOR -->
                <field name="proforma_ref" string="Proforma"/>
                <field name="vendor_id" string="Proveedor"/>
                <field name="vendor_country_id" string="País" optional="hide"/>
                <field name="transit_days" string="Tiempo Tránsito"/>
                
                <!-- 5. METRAJES -->
                <field name="qty_proforma" string="Metraje Proforma" optional="hide"/>
                <field name="qty_original_demand" string="Metraje Pedido Orig." optional="hide"/>
                
                <!-- 6. PRODUCTO -->
                <field name="product_id" string="Descripción"/>
                <field name="product_uom_qty" string="M2 Embarcados" sum="Total M2"/>
                <field name="price_unit" string="Precio / M2"/>
                <field name="currency_id" string="Moneda" invisible="1"/>
                
                <!-- 7. LOGÍSTICA -->
                <field name="container_number" string="Contenedor"/>
                <field name="shipping_line" string="Naviera"/>
                <field name="bl_number" string="Factura de Carga"/>
                
                <!-- 8. FECHAS -->
                <field name="write_date" string="Última Revisión" widget="date" optional="hide"/>
                <field name="etd" string="ETD"/>
                <field name="eta" string="ETA"/>
                <field name="arrival_date" string="Llegada Real" optional="show"/>
                
                <!-- 9. COMENTARIOS -->
                <field name="notes" string="COMENTARIOS" placeholder="Escriba notas..."/>
                
                <!-- Acciones -->
                <button name="action_reassign_wizard" icon="fa-refresh" title="Reasignar" type="object"/>
            </list>
        </field>
    </record>

    <!-- ACCIÓN: Sábana de Seguimiento (Lista Detallada) -->
    <record id="action_transit_tracking_sheet" model="ir.actions.act_window">
        <field name="name">Sábana de Seguimiento</field>
        <field name="res_model">stock.transit.line</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_transit_line_tracking_list"/>
        <!-- CORRECCIÓN: Se eliminó la línea <field name="search_view_id" ... /> que causaba el error -->
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Aquí verá el seguimiento detallado tipo Excel.
            </p>
        </field>
    </record>

    <!-- ACCIÓN: Tablero de Viajes (Original Agrupado) -->
    <record id="action_stock_transit_voyage_main" model="ir.actions.act_window">
        <field name="name">Viajes (Agrupado)</field>
        <field name="res_model">stock.transit.voyage</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Gestión por Viaje/Contenedor
            </p>
        </field>
    </record>

    <!-- ========================================================== -->
    <!-- MENÚS                                                     -->
    <!-- ========================================================== -->

    <menuitem id="menu_transit_root"
              name="Torre de Control"
              web_icon="stock_transit_allocation,static/description/icon.png"
              sequence="10"
              groups="stock_transit_allocation.group_transit_user"/>

    <!-- 1. Menú "Sábana" (Vista Excel) - PRIORIDAD 1 -->
    <menuitem id="menu_transit_sheet"
              name="Sábana de Seguimiento"
              parent="menu_transit_root"
              action="action_transit_tracking_sheet"
              sequence="1"/>

    <!-- 2. Menú "Viajes" (Vista Kanban/Agrupada) -->
    <menuitem id="menu_transit_voyage_dashboard"
              name="Viajes y Contenedores"
              parent="menu_transit_root"
              action="action_stock_transit_voyage_main"
              sequence="2"/>

</odoo>